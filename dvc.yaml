stages:
  train:
    cmd: python3 src/train.py --data_path data/chest_xray --epochs 10 --batch_size 32
    deps:
      - data/chest_xray
      - src/train.py
    outs:
      - models/cancer_detection_model.h5

